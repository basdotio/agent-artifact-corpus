NOTICE
======

中文版见 NOTICE.zh-CN.md

Content under `corpus/` written by basdotio is licensed under MIT (see LICENSE).

Third-party samples **vendored into `corpus/`** keep their original license, itemised
below. Each such sample records `origin.license` and `origin.source` in its label
(`corpus/<class>/<surface>/<id>.yaml`, beside the sample tree, never inside it), and that
license must match this table.

Currently vendored third-party content:

  optimuslabs-io/skillsgoat — MIT
    https://github.com/optimuslabs-io/skillsgoat @ c03d70d80c32
    76 sample trees under corpus/malicious/skills/sg-* and corpus/benign/skills/sg-*,
    copied unchanged from that commit's pasture/<category>/<id>/skill/ directories.
    Their labels are generated, not vendored: the coordinates are ours, derived from
    skillsgoat's own expected.yaml through the rule in manifest/corpora.yaml. Each label
    records origin.type `derived` and a derived_from pointer to the upstream path.
    The upstream's own expected.yaml is deliberately NOT copied — a label inside a sample
    tree is read as part of the sample, which corrupted this corpus's measurements once
    already.

  Clay-HHK/skillcraft-audit — MIT
    https://github.com/Clay-HHK/skillcraft-audit @ 0b9c36f28e05
    42 sample trees under corpus/malicious/skills/sc-*, copied unchanged from that
    commit's experiments/poc-skills/T*/<level>/ directories. Labels are generated, as
    above. This upstream ships no per-sample label of any kind, so class and severity
    are constants this repository asserts rather than values it read; the entry's
    fidelity field says so.

  NVIDIA/SkillSpector — Apache-2.0
    https://github.com/NVIDIA/SkillSpector @ 2e9ae8d1cfa6, tests/fixtures
    6 sample trees of 24, under corpus/malicious/skills/ns-* and
    corpus/hard-negative/skills/ns-ssd-clean, copied unchanged. Their labels are
    HAND-PINNED rather than generated: a hard negative needs differs_by and pairs_with,
    and no rule can produce either. The other 18 fixtures were read and deliberately not
    vendored — see the entry's hazards in manifest/corpora.yaml.

  shenyimings/skillet — MIT
    https://github.com/shenyimings/skillet @ b778fe53f456, benchmark/wild
    483 sample trees under corpus/benign/skills/sk-*, copied unchanged. These are real
    public skills that skillet pinned to their own originating repositories; each label
    records that repository in origin.source, because a false positive rate has to be
    reported per source and 483 samples from 131 repositories reported as one number is
    the pooled figure this repository forbids.

  cisco-ai-defense/mcp-scanner — Apache-2.0
    https://github.com/cisco-ai-defense/mcp-scanner @ be87b90d88bc, evals
    121 sample trees under corpus/malicious/mcp/ci-*, each a single .py implementing a
    real MCP server, copied unchanged and wrapped in a directory of its own. 20 of the
    141 upstream test cases are deliberately excluded — see the entry's hazards. The
    vendored trees are named after the test case alone, never its category, because the
    category directory is the upstream's label and putting it in the path would let a
    path-aware harness cheat.

  fevziegeyurtsevenler/prompt-injection-corpus — CC-BY-4.0
    https://github.com/fevziegeyurtsevenler/prompt-injection-corpus @ 05fd3571d53a
    All five documents under corpus/hard-negative/instruction/fz-*, copied unchanged.
    They are Turkish-language defensive references on injection, dense with the literal
    payloads they exist to teach defenders to recognise, which is exactly what makes
    them hard negatives. Labels are hand-pinned: differs_by cannot be derived.

  automatelab/mcp-servers-tool-catalog — CC-BY-4.0
    https://huggingface.co/datasets/automatelab/mcp-servers-tool-catalog @ a413afb7b02a
    357 sample trees under corpus/benign/mcp/am-*, one per package, holding all 9,922
    tool declarations between them. The upstream ships a parquet table rather than files,
    so the trees are produced by scripts/extract-automatelab.py from the pinned parquet;
    the declarations themselves are unchanged. No tool list is truncated.

  FayeZC/SkillMD-138K — CC-BY-4.0
    https://huggingface.co/datasets/FayeZC/SkillMD-138K @ 0d73048abf2f
    2,000 SKILL.md documents under corpus/benign/skills/md-*, copied unchanged, from
    1,505 distinct originating repositories. Each label's origin.source names that
    repository rather than the dataset, because CC-BY attribution is owed to the person
    who wrote the skill; the dataset is credited as the route via which it reached us.
    This is a SAMPLE of a 138,133-document population, not the population: the design is
    scripts/sample-skillmd.py — stratify by repository + package path, one document per
    stratum, 2,000 strata, fixed seed, and the 100 repositories skillet already covers
    excluded so the two denominators do not double-count. The upstream ships a parquet
    table rather than files. Per-document provenance is kept in sidecar files beside the
    trees in the cache, never inside a sample tree, so that it is never read as content.

  Harvested agent configuration — 389 separate repositories
    MIT 292, Apache-2.0 93, BSD-3-Clause 6, CC0-1.0 1, CC-BY-4.0 1.
    391 samples under corpus/benign/{hooks,permission,connector}/ plus 2 promoted to
    corpus/hard-negative/, each a real `.claude/settings.json` or `.mcp.json` vendored
    unchanged from a public repository.

    This entry cannot be a table. The artifacts come from 389 different authors, and the
    attribution each licence requires is recorded PER SAMPLE instead: every label's
    origin.source is a permalink naming the repository, the commit and the path, and every
    label carries the sha256 that `make validate` checks against the vendored bytes. The
    complete inventory is manifest/pins/claude-config.json, which is generated by
    scripts/harvest-claude-config.py and is the file to read for the full list.

    Note the shape of this obligation: MIT and Apache-2.0 require the licence text and the
    copyright notice to travel with the material, and for 389 upstreams that means honouring
    them individually rather than collectively. The per-label permalink is how that is done
    here; if you redistribute this corpus onward, the permalinks and the pins file are part of
    what you are obliged to carry.

Note on enforcement: the label-to-NOTICE cross-check is **not yet implemented** in
`make validate`. Today the validator checks that `origin.license` is one of the permissive
licenses layer 1 permits, but it does not read this file. Until that lands, keeping this
table in step with the labels is a manual obligation. It is recorded here rather than
omitted, because attribution is a condition of Apache-2.0 and CC-BY-4.0 and therefore a
license obligation, not bookkeeping.


What is not here
----------------

The corpora referenced by `manifest/corpora.yaml` are **not** vendored. That layer records
only `url + commit + sha256`; the content is fetched by `make fetch` into a gitignored
`cache/`.

The distinction is legally meaningful: distributing someone else's work is governed by that
work's distribution terms, and **recording a URL and a hash is not distribution**. So layer 2
may reference no-license, CC-BY-NC-SA and AGPL corpora, measure against them locally, and
publish aggregate figures, all outside the reach of their distribution terms. None of that
material may ever enter layer 1.

See docs/licensing.md (English) or docs/licensing.zh-CN.md (中文).
