# NOTICE

本仓库 `corpus/` 下由 basdotio 编写的内容依 MIT 授权（见 LICENSE）。

`corpus/` 下**从第三方 vendored 进来的**样本各自保留原许可，逐项列在下面。
每个这样的样本目录里必须有一个 `_label.yaml`，其 `origin.license` 字段
与此表一致；`make validate` 会校验一致性。

当前 vendored 的第三方内容：

  （暂无。第 1 层目前只有 basdotio 自己编写的样本。）

---

## 不在这里的东西

`external/manifest.yaml` 引用的语料**没有** vendored —— 那一层只记录
`url + commit + sha256`，内容由 `make fetch` 拉到 gitignore 的 `cache/`。

这个区分是有法律意义的：分发别人的作品受其许可的分发条款约束，而**记录一个 URL 和
一个哈希不是分发**。所以第 2 层可以包含无 license、CC-BY-NC-SA、AGPL 的语料并在本地
测量、发布聚合数字，而这些语料一律不允许进入第 1 层。

详见 doc/licensing.zh-CN.md。
