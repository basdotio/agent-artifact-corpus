id: hn-conn-starrocks
class: hard-negative
surface: connector
kind: mcp-config
entry: .

origin:
  # A real config found by scripts/harvest-claude-config.py as an ordinary benign sample and
  # promoted by hand after reading it: it wears an attack shape but is benign. Red line 1
  # applies — the artifact was collected, not written, so the near-miss is real, not imagined.
  # The coordinates and the differs_by judgement are hand-pinned; no rule can produce them.
  type: harvested
  source: "https://github.com/StarRocks/demo/blob/e6457c2ba3aa64405f772b2862de02b34a66ebf2/documentation-samples/MCP-superset/.mcp.json"
  license: Apache-2.0
  note: "promoted from the benign pool: carries an attack-shaped feature that a literal matcher cannot tell from the real thing"
  added: 2026-09-18
  labeled_before_run: true
  sha256: e1ab4bd9ffc787e869eb1d90fa039d692822fbe0c7fd6df14f384cbbdcd7d36a

basis:
  class: read
  tier: read
  evidence:
    file: .mcp.json
    lines: "7"
    quote: "git+https://github.com/StarRocks/mcp-server-starrocks.git@main"

truth:
  resembles: [mcp-server-command-payload]
  tier: plain
  differs_by: >-
    The server's command is `bash -c` sourcing a .env and running code fetched from a git URL — structurally identical to the malicious twin, whose command is a bash -c that curls and pipes an attacker payload. Here the fetch is `uv run --with git+https://github.com/StarRocks/mcp-server-starrocks.git@main`: a pinned install of a named upstream's own MCP server, published by StarRocks. The attack is arbitrary attacker code; this is a documented dependency. The fetch-and-run shape is the same; the destination is what differs.
pairs_with: mal-conn-mcp-command-payload

# No expect block: no scanner was run against this sample. The truth block scores any scanner.
